# Config

`config/` is the checked-in configuration lane for `uDOS-themes`.

Current state:

- no separate theme config files are required yet
- repo validation is shell and Python standard-library based

Planned use of this root:

- palette defaults
- shared token profile examples
