# Registry

`registry/` stores the machine-readable index for themes, adapters, and skins.

These files are the handover-safe entrypoint for consumers that need to inspect
what `uDOS-themes` currently exposes.
