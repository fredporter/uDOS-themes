# Examples

`examples/` is the smallest inspectable walkthrough lane for `uDOS-themes`.

Current example lanes:

- `basic-theme.json` for the smallest public theme contract example

Phase 1 rule:

- keep examples public and reusable
- keep private branding outside this repo
