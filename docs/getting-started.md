# uDOS-themes Getting Started

1. Define tokens or assets in `src/`.
2. Review `docs/activation.md`.
3. Document usage patterns in `docs/`.
4. Run `scripts/run-theme-checks.sh`.
5. Add examples that show cross-repo reuse.
6. Keep branding boundaries explicit.
